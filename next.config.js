/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.shopify.com', 'images.unsplash.com', 'www.thespruce.com', 'img.choice.com.au', 'electroluxattackcare.id', 'okdiario.com', 'activedetergent.com'],
  },
}

module.exports = nextConfig
